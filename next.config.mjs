/** @type {import('next').NextConfig} */
const nextConfig = {
    output : "export",
    basePath: '/kerlin_campos',
    assetPrefix: '/kerlin_campos/',
    images: {
        unoptimized: true
    }
};

export default nextConfig;
